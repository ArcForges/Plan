# Single-task execution command

```text
Read C:\MyFile\Projects\Plan\arcforges-implementation.md completely and follow it to complete only its Current task, including execution-policy.md.
Verify relevant facts, finish research and decisions, then establish one complete ordered plan before editing. Execute continuously using retained Git worktrees; append to a related open PR or create a new one. Repair authoritative documentation and AGENTS conflicts.
Independent repositories may use subagents; serialize CPU-heavy local builds. No macOS CI, hosted device/emulator/GUI/browser E2E, live service/inference, installed-consumer or public-release upgrade tests. No routine public artifact downloads, repeated archive/hash comparison or post-merge runtime cycles. Keep necessary Windows/Linux compilation/AOT/packaging, targeted offline/static/security checks and required signing/licence/lock integrity. Runtime tests are affected-scope local opt-in using existing environments, once; no validation-driven toolchain reinstall.
Use normal networking, no proxy7890 and no wsl.exe wrappers. On a network failure, stop and report the operation. Do not create tags or republish solely to verify.
Review every PR and fix findings. Merge automatically after retained applicable CI is green; docs-only PRs without CI merge after review. Post-merge: expected commit, required build/publication/deployment status and clean primary fast-forward only. Keep branches/worktrees, report actual results and untested coverage, then stop without another substep.
```
